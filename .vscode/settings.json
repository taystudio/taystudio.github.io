{
  "liveServer.settings.port": 5506,
  "//": "blog/ 는 전부 build.py 생성물(글·홈·태그·카테고리·_source 초안). 게재=저장+빌드 시 이 파일들이 다시 써지는데, Live Server 가 그걸 감지해 페이지를 반복 새로고침(뺑뺑이)하며 작성/게재가 깨진다. blog/** 전체를 reload 감시에서 제외 (파일 서빙은 그대로 됨, 변경은 수동 새로고침으로 확인). 도구 사이트(루트)는 정상 reload.",
  "liveServer.settings.ignoreFiles": [
    ".vscode/**",
    "**/*.scss",
    "**/*.sass",
    "**/*.ts",
    "blog/**",
    "**/blog/**"
  ]
}
